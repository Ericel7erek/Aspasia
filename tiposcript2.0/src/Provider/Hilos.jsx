import { useParams } from "react-router-dom";

const Hilos = () => {
  const id = useParams();
  return (
    <div>
      <input type="text" />
      <button></button>
    </div>
  );
};

export default Hilos;
